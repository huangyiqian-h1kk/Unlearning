# Reusable configuration components

These JSON registries give future configurations stable model, method, dataset,
and protocol references. They are descriptive contracts, not dependency or
dataset installers.

Dataset IDs resolve through the ClinicIA registry and protected paper
inventory. Model identifiers still require upstream access and terms review.
Method ownership keeps project ConRep distinct from OpenUnlearning-derived
baselines.
