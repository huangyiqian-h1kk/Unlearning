# Compact sweep definitions

Sweep files describe finite review matrices without materializing per-run
configuration files, job scripts, logs, checkpoints, or outputs. Phase 3D-3's
matrix is explicitly review-only and does not claim that all combinations are
scientifically or operationally ready.
